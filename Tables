#include <stdio.h>

void main ()
{
int N,i,result;

i=1;
printf("Please enter the number :");
scanf("%d",&N);

while (i<11)
{
    result = N*i;
    
    printf("%d * %d = %d\n",N,i,result);
    i=i+1;
}
